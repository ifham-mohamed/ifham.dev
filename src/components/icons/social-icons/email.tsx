import { MailIcon as LucideMailIcon } from "lucide-react";
import type { SVGProps } from "react";

export const EmailIcon = (props: SVGProps<SVGSVGElement>) => (
  <LucideMailIcon {...props} />
);

export default EmailIcon;
